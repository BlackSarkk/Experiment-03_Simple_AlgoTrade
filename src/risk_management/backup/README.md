# Historical / reference RiskManager only.

Not used or imported by production code.

- `baseline_old.py` — exact pre-Phase-2 `BaselineRiskManager`, recovered verbatim from Git
  (`git show HEAD:src/risk_management/baseline.py`). Do not modify.
- `old_risk_values.json` — the risk configuration values that belonged to that implementation.

The active implementation is `src/risk_management/baseline.py`.
The active policy values are `configs/riskmanager.json`.
